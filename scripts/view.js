define(function (require) {
	var picture = require('./picture');
	var active_layer = require('./layer');

	//in milliseconds
	var active_time = 0;

	//in percentages
	var zoom = 100;

	//TODO panning
});
